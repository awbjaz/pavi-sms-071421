<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="inherit_account_move_view_form" model="ir.ui.view">
        <field name="name">inherit.account.move.view.form</field>
        <field name="model">account.move.form</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="button_send_sms" type="action" string="Send SMS Reminder" />
            </xpath>
        </field>
    </record>
</odoo>